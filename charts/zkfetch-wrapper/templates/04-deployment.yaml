apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "fullname" . }}
  labels:
    {{- include "labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8003
              protocol: TCP
          env:
            - name: NODE_ENV
              valueFrom:
                configMapKeyRef:
                  name: zkfetch-wrapper-config
                  key: NODE_ENV
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: zkfetch-wrapper-config
                  key: PORT
            - name: USE_TEE
              valueFrom:
                configMapKeyRef:
                  name: zkfetch-wrapper-config
                  key: USE_TEE
            - name: RECLAIM_ADDRESS
              valueFrom:
                configMapKeyRef:
                  name: zkfetch-wrapper-config
                  key: RECLAIM_ADDRESS
            - name: RECLAIM_OPTIMISM_ADDRESS
              valueFrom:
                configMapKeyRef:
                  name: zkfetch-wrapper-config
                  key: RECLAIM_OPTIMISM_ADDRESS
            - name: RECLAIM_APP_ID
              valueFrom:
                secretKeyRef:
                  name: zkfetch-wrapper
                  key: RECLAIM_APP_ID
            - name: RECLAIM_APP_SECRET     
              valueFrom:
                secretKeyRef:
                  name: zkfetch-wrapper
                  key: RECLAIM_APP_SECRET 
            - name: SEPOLIA_RPC_URL
              valueFrom:
                secretKeyRef:
                  name: zkfetch-wrapper
                  key: SEPOLIA_RPC_URL
            - name: OPTIMISM_SEPOLIA_RPC
              valueFrom:
                secretKeyRef:
                  name: zkfetch-wrapper
                  key: OPTIMISM_SEPOLIA_RPC
            - name: PRIVATE_KEY
              valueFrom:
                secretKeyRef:
                  name: zkfetch-wrapper
                  key: PRIVATE_KEY
          livenessProbe:
            httpGet:
              path: /health
              port: http
          readinessProbe:
            httpGet:
              path: /health
              port: http
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}